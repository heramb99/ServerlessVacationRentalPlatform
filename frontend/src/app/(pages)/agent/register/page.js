import React from 'react';
import RegisterContainer from '../../components/register/RegisterContainer';

const Register = () => {
  return <RegisterContainer role={'agent'} />;
};

export default Register;
